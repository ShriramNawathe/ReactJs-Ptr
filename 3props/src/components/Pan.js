import React from 'react'

function Pan(props) {
  return (
    <div>pan1
        <h1>{props.name}</h1>
        <h1>{props.age}</h1>
        <h1>{props.gmail}</h1>
    </div>
  )
}

export default Pan